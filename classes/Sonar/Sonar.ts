class Sonar {
    // Two modes, active and passive. 
    // Active mode sends out a ping that gets reflected
    //  This will require a depth modified sound profile coefficient
    //  And alert everything to the fact that you're there
    // Passive mode just listens for things
    ping(): void {}
    listen(): void {}
}